// Assignability Error

let message= "Hello Wrold!";
message= 6;  
console.log(message);

// Line 4 will cause Assignability error becuase in message we have stored a string data type so if we want to change it we can only give a string data type. So number data type is causing error here